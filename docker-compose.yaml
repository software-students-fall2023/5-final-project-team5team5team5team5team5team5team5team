version: "3"
services:
  web_app:
    build: ./web_app
    ports:
      - "5000:5000"
    environment:
      - URI=${MONGO_URI}
      - DATABASE=accounts
      - COLLECTION=users
      - FLASK_SECRET_KEY=${FLASK_SECRET_KEY}
      - SPOON_SECRET=${SPOON_SECRET}

volumes:
  mongodb-data: